# Browser Selector - Hardware

This is the Arduino code for this project.

The code sends a `<` or `>` character with no encapsulation over USB serial as a rotary encoder is turned with debounce and little else.